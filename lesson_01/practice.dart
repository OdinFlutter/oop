// УРОК 1: Практика — Класи та об'єкти
// Виконуйте завдання поступово, розкоментовуючи taskX() по черзі
// Запуск: dart practice.dart

import '../progress.dart';

void main() {
  // Ініціалізуємо прогрес-трекер
  ProgressTracker.initialize();
  
  // Показуємо прогрес курсу
  ProgressTracker.displayProgress();
  
  print('\n🎯 ПРАКТИКА — УРОК 1: КЛАСИ ТА ОБ'ЄКТИ');
  print('=' * 50);

  // Розкоментуйте потрібне завдання для виконання
  // task1();
  // task2();
  // task3();
  // task4();
  // task5();

  print('\n💡 Порада: Розкоментуйте task1() для початку роботи!');
  print('📚 Перевірте solutions.dart для порівняння після виконання.');
}


// =============================================================================
// ЗАВДАННЯ 1: Клас "Команда" (Team)
// =============================================================================

void task1() {
  print('ЗАВДАННЯ 1: Клас "Команда" (Team)');
  print('=' * 30);

  /*
  🔎 Що таке клас?
  Клас — це "шаблон" для створення об'єктів. Наприклад, клас Team описує, як виглядає команда Формули 1.

  🏁 Навіщо це потрібно?
  Завдяки класам ми можемо створювати багато різних команд, які мають однакову структуру, але різні дані.

  ✅ Що треба зробити:
  1. Створи клас Team (Команда), який описує команду Формули 1.
     - name (String): назва команди (наприклад, "Ferrari")
     - country (String): країна (наприклад, "Італія")
     - foundedYear (int): рік заснування (наприклад, 1950)
     - championships (int): кількість чемпіонств (початково 0)

  2. Додай конструктор, який приймає name, country, foundedYear.
     championships має автоматично ставати 0 (бо команда ще не виграла).

  3. Додай метод displayTeamInfo(), який виводить інформацію про команду у зручному вигляді.
     Наприклад:
     Ferrari (Італія), заснована у 1950, чемпіонств: 0

  4. Додай метод winChampionship(), який збільшує championships на 1.
     Пояснення: коли команда виграє чемпіонат, цей метод треба викликати.

  🧩 Пояснення:
  - Клас — це як креслення машини, а об'єкт — це вже конкретна машина.
  - Методи — це "дії", які може виконувати об'єкт (наприклад, команда може виграти чемпіонат).
  - Конструктор — це "інструкція", як створити нову команду.

  💡 Підказка:
  championships = 0 треба встановити у конструкторі, бо на старті команда ще нічого не виграла.

  🔨 Приклад використання:
    Team ferrari = Team('Ferrari', 'Італія', 1950);
    ferrari.displayTeamInfo(); // Ferrari (Італія), заснована у 1950, чемпіонств: 0
    ferrari.winChampionship();
    ferrari.displayTeamInfo(); // Ferrari (Італія), заснована у 1950, чемпіонств: 1

  -----------------------------------------------------------------------------
  🟢 ДУЖЕ ДЕТАЛЬНО: ЯК СТВОРИТИ КОНСТРУКТОР У DART

  1. Що таке конструктор?
     Конструктор — це спеціальний метод, який викликається, коли ми створюємо новий об'єкт класу.
     Він має таку ж назву, як і клас.

  2. Як виглядає конструктор у Dart?
     Ось базовий приклад:

     class Team {
       String name;
       String country;
       int foundedYear;
       int championships;

       // Це конструктор:
       Team(this.name, this.country, this.foundedYear) {
         championships = 0; // при створенні команди чемпіонств ще немає
       }
     }

     // Пояснення:
     // Team(...) — це конструктор. В дужках — параметри, які треба передати при створенні об'єкта.
     // this.name означає, що ми присвоюємо значення властивості name цього об'єкта.
     // championships = 0 — встановлюємо початкове значення.

  3. Чим конструктор відрізняється від методу?
     - Конструктор викликається автоматично при створенні об'єкта: Team('Ferrari', 'Італія', 1950)
     - Метод — це дія, яку можна викликати у будь-який момент: ferrari.displayTeamInfo()

  4. Як створити об'єкт через конструктор?
     Team mercedes = Team('Mercedes', 'Німеччина', 1954);
     // Тепер mercedes.name == 'Mercedes', mercedes.championships == 0

  5. Що таке this?
     this.name означає "властивість name цього об'єкта". Це потрібно, щоб відрізнити властивість класу від параметра конструктора.

  6. Ще приклад з коментарями:

     class Team {
       String name;
       String country;
       int foundedYear;
       int championships;

       // Конструктор з поясненнями
       Team(this.name, this.country, this.foundedYear) {
         // championships завжди 0 при створенні
         championships = 0;
       }

       void displayTeamInfo() {
         print('Команда: ' + name + ' (' + country + '), заснована у ' + foundedYear.toString() + ', чемпіонств: ' + championships.toString());
       }

       void winChampionship() {
         championships = championships + 1;
       }
     }

     void main() {
       Team ferrari = Team('Ferrari', 'Італія', 1950);
       ferrari.displayTeamInfo(); // Ferrari (Італія), заснована у 1950, чемпіонств: 0
       ferrari.winChampionship();
       ferrari.displayTeamInfo(); // Ferrari (Італія), заснована у 1950, чемпіонств: 1
     }

  -----------------------------------------------------------------------------
  Якщо щось не зрозуміло — спробуй скопіювати цей приклад у solutions.dart і змінити дані. Так легше зрозуміти, як працює конструктор!
  */

  // Тут створи свій клас та протестуй його, як у прикладі вище

  // Позначаємо завдання як виконане
  ProgressTracker.markTaskCompleted('lesson_01', 'task1');
  print('\n✅ Завдання 1 позначено як виконане!');
  print('📊 Прогрес оновлено автоматично.');

  print('');
}

// =============================================================================
// ЗАВДАННЯ 2: Іменовані конструктори для "Траса" (Circuit)
// =============================================================================

void task2() {
  print('ЗАВДАННЯ 2: Іменовані конструктори для "Траса" (Circuit)');
  print('=' * 50);

  /*
  🔎 Що таке іменований конструктор?
  Іменований конструктор — це спеціальний тип конструктора, який дозволяє створювати об'єкти з певними стандартними параметрами. Наприклад, для різних типів трас у Формулі 1 (міська, овальна тощо).

  🏁 Навіщо це потрібно?
  Це зручно, коли треба створювати багато схожих об'єктів з різними стандартними параметрами. Наприклад, всі овальні траси мають 4 повороти, а міські — 90.

  ✅ Що треба зробити:
  1. Створи клас Circuit (Траса) з такими властивостями:
     - name (String): назва траси (наприклад, "Монако")
     - country (String): країна (наприклад, "Монако")
     - length (double): довжина в км (наприклад, 3.337)
     - corners (int): кількість поворотів (наприклад, 19)

  2. Додай конструктори:
     - Основний: Circuit(name, country, length, corners)
     - Circuit.street() — для міських трас (corners = 90)
     - Circuit.oval() — для овалів (corners = 4)

  3. Додай метод displayCircuitInfo(), який виводить інформацію про трасу у зручному вигляді.
     Наприклад:
     Монако (Монако), 3.337 км, поворотів: 19

  🧩 Пояснення:
  - Іменовані конструктори — це як шаблони для різних типів трас.
  - Це допомагає уникати дублювання коду та робить створення об'єктів простішим.

  💡 Підказка:
  Для іменованого конструктора використовуй синтаксис:
    Circuit.street(String name, String country, double length) : this(name, country, length, 90);

  🔨 Покроковий приклад:

    class Circuit {
      String name;
      String country;
      double length;
      int corners;

      // Основний конструктор
      Circuit(this.name, this.country, this.length, this.corners);

      // Іменований конструктор для міської траси
      Circuit.street(String name, String country, double length)
        : this(name, country, length, 90);

      // Іменований конструктор для овальної траси
      Circuit.oval(String name, String country, double length)
        : this(name, country, length, 4);

      void displayCircuitInfo() {
        print('Траса: ' + name + ' (' + country + '), ' + length.toString() + ' км, поворотів: ' + corners.toString());
      }
    }

    void main() {
      Circuit monaco = Circuit('Монако', 'Монако', 3.337, 19);
      Circuit street = Circuit.street('Лас-Вегас', 'США', 6.2);
      Circuit oval = Circuit.oval('Індіанаполіс', 'США', 4.0);
      monaco.displayCircuitInfo();
      street.displayCircuitInfo();
      oval.displayCircuitInfo();
    }

  -----------------------------------------------------------------------------
  Якщо не зрозуміло — скопіюй цей приклад у solutions.dart і змінюй дані. Так легше зрозуміти, як працюють іменовані конструктори!
  */

  // Тут створи свій клас та протестуй його, як у прикладі вище

  print('');

  // Позначаємо завдання як виконане
  ProgressTracker.markTaskCompleted('lesson_01', 'task2');
  print('\n✅ Завдання 2 позначено як виконане!');
  print('📊 Прогрес оновлено автоматично.');

  print('');}

// =============================================================================
// ЗАВДАННЯ 3: Клас з обчислюваною властивістю (RaceResult)
// =============================================================================

void task3() {
  print('ЗАВДАННЯ 3: Клас з обчислюваною властивістю (RaceResult)');
  print('=' * 40);

  /*
  🔎 Що таке обчислювана властивість (getter)?
  Обчислювана властивість (getter) — це властивість, яка не зберігається напряму, а рахується автоматично при кожному зверненні. Наприклад, кількість балів за гонку залежить від місця та найшвидшого кола.

  🏁 Навіщо це потрібно?
  Це дозволяє завжди отримувати актуальне значення, яке залежить від інших властивостей. Не треба вручну рахувати бали — все рахується автоматично.

  ✅ Що треба зробити:
  1. Створи клас RaceResult (Результат гонки):
     - driverName (String): ім'я пілота
     - position (int): місце (1-20)
     - fastestLap (bool): чи був найшвидший круг

  2. Додай обчислювану властивість points (getter):
     - 1 місце: 25 балів (+1 за найшвидше коло)
     - 2 місце: 18 балів
     - 3 місце: 15 балів
     - 4 місце: 12 балів
     - 5 місце: 10 балів
     - 6 місце: 8 балів
     - 7 місце: 6 балів
     - 8 місце: 4 бали
     - 9 місце: 2 бали
     - 10 місце: 1 бал
     - 11+ місце: 0 балів

  3. Додай метод displayResult(), який виводить результат гонки у вигляді:
     Макс Ферстаппен — 1 місце, 26 балів (найшвидше коло)

  🧩 Пояснення:
  - getter — це як формула, яка автоматично рахує значення
  - Це зручно для балів, бо вони залежать від місця та найшвидшого кола

  💡 Підказка:
  getter оголошується так:
    int get points { ... }

  🔨 Покроковий приклад:

    class RaceResult {
      String driverName;
      int position;
      bool fastestLap;

      RaceResult(this.driverName, this.position, this.fastestLap);

      // Обчислювана властивість (getter)
      int get points {
        int pts = 0;
        switch (position) {
          case 1: pts = 25; break;
          case 2: pts = 18; break;
          case 3: pts = 15; break;
          case 4: pts = 12; break;
          case 5: pts = 10; break;
          case 6: pts = 8; break;
          case 7: pts = 6; break;
          case 8: pts = 4; break;
          case 9: pts = 2; break;
          case 10: pts = 1; break;
          default: pts = 0;
        }
        if (fastestLap && position <= 10) pts += 1;
        return pts;
      }

      void displayResult() {
        String msg = driverName + ' — ' + position.toString() + ' місце, ' + points.toString() + ' балів';
        if (fastestLap && position <= 10) msg += ' (найшвидше коло)';
        print(msg);
      }
    }

    void main() {
      RaceResult winner = RaceResult('Макс Ферстаппен', 1, true);
      winner.displayResult();
    }

  -----------------------------------------------------------------------------
  Якщо не зрозуміло — скопіюй цей приклад у solutions.dart і змінюй дані. Так легше зрозуміти, як працює getter!
  */

  // Тут створи свій клас та протестуй його, як у прикладі вище

  print('');

  // Позначаємо завдання як виконане
  ProgressTracker.markTaskCompleted('lesson_01', 'task3');
  print('\n✅ Завдання 3 позначено як виконане!');
  print('📊 Прогрес оновлено автоматично.');

  print('');}

// =============================================================================
// ЗАВДАННЯ 4: Взаємодія об'єктів (PitStop + Driver)
// =============================================================================

void task4() {
  print('ЗАВДАННЯ 4: Взаємодія об\'єктів (PitStop + Driver)');
  print('=' * 30);

  /*
  🔎 Що таке взаємодія об'єктів?
  Взаємодія об'єктів — це коли один об'єкт (наприклад, Driver) використовує інший (PitStop) для виконання дії. Це основа ООП: об'єкти співпрацюють між собою.

  🏁 Навіщо це потрібно?
  Це дозволяє моделювати реальні ситуації, де об'єкти співпрацюють (наприклад, пілот заїжджає на піт-стоп).

  ✅ Що треба зробити:
  1. Створи клас PitStop (Піт-стоп):
     - duration (double): тривалість у секундах
     - tyreBrand (String): марка шин
     - fuelAdded (double): скільки пального додали

  2. Створи клас Driver (Пілот):
     - name (String): ім'я
     - currentTyres (String): поточний тип шин
     - fuel (double): залишок пального
     - Метод performPitStop(PitStop pitStop): змінює шини, додає паливо, виводить інформацію

  3. Використай ці класи разом:
     Driver lando = Driver('Ландо Норріс', 'Medium', 50.0);
     PitStop quickStop = PitStop(2.3, 'Soft', 30.0);
     lando.performPitStop(quickStop);

  🧩 Пояснення:
  - Це і є об'єктно-орієнтованість: об'єкти взаємодіють між собою
  - Так легше моделювати реальні процеси

  💡 Підказка:
  У методі performPitStop зміни currentTyres та fuel відповідно до pitStop.

  🔨 Покроковий приклад:

    class PitStop {
      double duration;
      String tyreBrand;
      double fuelAdded;

      PitStop(this.duration, this.tyreBrand, this.fuelAdded);
    }

    class Driver {
      String name;
      String currentTyres;
      double fuel;

      Driver(this.name, this.currentTyres, this.fuel);

      void performPitStop(PitStop pitStop) {
        currentTyres = pitStop.tyreBrand;
        fuel += pitStop.fuelAdded;
        print(name + ' зробив піт-стоп: шини — ' + currentTyres + ', пального — ' + fuel.toString() + ' л, час: ' + pitStop.duration.toString() + ' сек');
      }
    }

    void main() {
      Driver lando = Driver('Ландо Норріс', 'Medium', 50.0);
      PitStop quickStop = PitStop(2.3, 'Soft', 30.0);
      lando.performPitStop(quickStop);
    }

  -----------------------------------------------------------------------------
  Якщо не зрозуміло — скопіюй цей приклад у solutions.dart і змінюй дані. Так легше зрозуміти, як об'єкти взаємодіють між собою!
  */

  // Тут створи свої класи та протестуй їх, як у прикладі вище

  print('');

  // Позначаємо завдання як виконане
  ProgressTracker.markTaskCompleted('lesson_01', 'task4');
  print('\n✅ Завдання 4 позначено як виконане!');
  print('📊 Прогрес оновлено автоматично.');

  print('');}

// =============================================================================
// ЗАВДАННЯ 5: Симулятор кваліфікації (QualifyingSession)
// =============================================================================

void task5() {
  print('ЗАВДАННЯ 5: Симулятор кваліфікації (QualifyingSession)');
  print('=' * 40);

  /*
  🔎 Що таке симуляція?
  Симуляція — це коли ми імітуємо реальний процес у програмі. Наприклад, як проходить кваліфікація у Формулі 1.

  🏁 Навіщо це потрібно?
  Це дозволяє тренувати логіку, працювати зі списками, випадковими числами, взаємодією об'єктів. Такі завдання часто зустрічаються у реальних проектах.

  ✅ Що треба зробити:
  1. Створи клас QualifyingSession (Кваліфікація):
     - circuit (String): назва траси
     - weather (String): погода
     - List<Driver> drivers: список пілотів

  2. Додай методи:
     - addDriver(Driver driver): додати пілота
     - simulateSession(): симуляція кваліфікації (призначає випадкові часи)
     - displayResults(): виводить результати

  3. Використай Driver і Circuit з попередніх завдань.
     QualifyingSession quali = QualifyingSession('Сільверстоун', 'Сонячно');
     quali.addDriver(Driver('Льюїс Хемілтон', 44, 'Велика Британія'));
     quali.simulateSession();
     quali.displayResults();

  🧩 Пояснення:
  - Симуляція — це чудовий спосіб потренуватися працювати з об'єктами, списками, випадковістю
  - Такі завдання часто зустрічаються у реальних проектах

  💡 Підказка:
  Для випадкових чисел використовуй Random з dart:math

  🔨 Покроковий приклад:

    import 'dart:math';

    class Driver {
      String name;
      int number;
      String country;
      double qualifyingTime;

      Driver(this.name, this.number, this.country);
    }

    class QualifyingSession {
      String circuit;
      String weather;
      List<Driver> drivers = [];
      Random random = Random();

      QualifyingSession(this.circuit, this.weather);

      void addDriver(Driver driver) {
        drivers.add(driver);
      }

      void simulateSession() {
        for (var driver in drivers) {
          // Генеруємо випадковий час від 1:25.000 до 1:30.000
          driver.qualifyingTime = 85 + random.nextDouble() * 5;
        }
        drivers.sort((a, b) => a.qualifyingTime.compareTo(b.qualifyingTime));
      }

      void displayResults() {
        print('Результати кваліфікації на трасі ' + circuit + ' (' + weather + '):');
        for (int i = 0; i < drivers.length; i++) {
          var d = drivers[i];
          print('${i + 1}. ${d.name} (${d.country}) — ${d.qualifyingTime.toStringAsFixed(3)} сек');
        }
      }
    }

    void main() {
      QualifyingSession quali = QualifyingSession('Сільверстоун', 'Сонячно');
      quali.addDriver(Driver('Льюїс Хемілтон', 44, 'Велика Британія'));
      quali.addDriver(Driver('Макс Ферстаппен', 1, 'Нідерланди'));
      quali.simulateSession();
      quali.displayResults();
    }

  -----------------------------------------------------------------------------
  Якщо не зрозуміло — скопіюй цей приклад у solutions.dart і змінюй дані. Так легше зрозуміти, як працює симуляція!
  */

  // Тут створи свій клас та протестуй його, як у прикладі вище

  print('');

  // Позначаємо завдання як виконане
  ProgressTracker.markTaskCompleted('lesson_01', 'task5');
  print('\n✅ Завдання 5 позначено як виконане!');
  print('📊 Прогрес оновлено автоматично.');

  print('');}

// =============================================================================
// ПІДКАЗКИ ДЛЯ НОВАЧКІВ
// =============================================================================

/*
ПІДКАЗКИ:

1. Завжди ініціалізуй всі властивості у конструкторі
2. Для короткого запису використовуй this.властивість
3. Іменовані конструктори — це зручно для різних типів об'єктів
4. Геттер (get) — це властивість, яка рахується автоматично
5. Методи роблять клас "живим" — додають поведінку

DART:
- Використовуй late для відкладеної ініціалізації
- final — якщо значення не змінюється після створення
- const — для констант
*/
